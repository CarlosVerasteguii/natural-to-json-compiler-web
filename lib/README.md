# Librerías Externas

Este directorio `lib/` se mantiene en la raíz del proyecto para cumplir estrictamente con los **requisitos académicos de la asignación**.

Sin embargo, siguiendo las **mejores prácticas de Next.js** y la arquitectura moderna de aplicaciones web, toda la lógica del compilador, utilidades y librerías internas se han ubicado en:

👉 **`src/lib/`**

Esto asegura una estructura de proyecto limpia donde el código fuente (`src`) está claramente separado de los archivos de configuración y documentación de la raíz.
